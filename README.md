# AiToolsHub

Static single-page hub: **100+** free in-browser utilities (including AI-focused helpers), plus `tools.html` and `sitemap.html`.

**Live site:** https://ygunoil-ai-tools-hub.vercel.app/

Deploy as a static site (e.g. Vercel). Canonical URLs in HTML point at the configured host.
